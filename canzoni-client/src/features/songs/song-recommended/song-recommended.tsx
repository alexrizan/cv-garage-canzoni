import {Info} from '../../../components';

const SongRecommended = () => {
  return (
    <Info
    message={'Эта вкладка временно недоступна'}
    header={'В ожидании идей'}
    iconName={'emoji_objects'}
    />

  )
}

export default SongRecommended;
